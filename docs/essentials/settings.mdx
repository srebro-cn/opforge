---
title: '全局设置'
description: 'Mintlify通过docs.json文件让您完全控制文档的外观和风格'
icon: 'iconfont icon-setting'
---

每个Mintlify站点都需要一个包含核心配置设置的`docs.json`文件。了解更多关于下面的[属性](#properties)。

## 属性

<ResponseField name="name" type="string" required>
项目名称。用于全局标题。

示例：`mintlify`

</ResponseField>

<ResponseField name="navigation" type="Navigation[]" required>
  包含该组内所有页面的组数组
  <Expandable title="Navigation">
    <ResponseField name="group" type="string">
    组的名称。

    示例：`Settings`

    </ResponseField>
    <ResponseField name="pages" type="string[]">
    作为页面的markdown文件的相对路径。

    示例：`["customization", "page"]`

    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="logo" type="string or object">
  logo图片的路径或包含"light"和"dark"模式logo图片路径的对象
  <Expandable title="Logo">
    <ResponseField name="light" type="string">
      浅色模式下logo的路径
    </ResponseField>
    <ResponseField name="dark" type="string">
      深色模式下logo的路径
    </ResponseField>
    <ResponseField name="href" type="string" default="/">
      点击logo链接到的位置
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="favicon" type="string">
  favicon图标的路径
</ResponseField>

<ResponseField name="colors" type="Colors">
  全局主题的十六进制颜色代码
  <Expandable title="Colors">
    <ResponseField name="primary" type="string" required>
      主要颜色。在浅色模式下最常用于突出显示的内容、章节标题、强调部分
    </ResponseField>
    <ResponseField name="light" type="string">
      深色模式的主要颜色。在深色模式下最常用于突出显示的内容、章节标题、强调部分
    </ResponseField>
    <ResponseField name="dark" type="string">
      重要按钮的主要颜色
    </ResponseField>
    <ResponseField name="background" type="object">
      浅色和深色模式下的背景颜色
      <Expandable title="Object">
        <ResponseField name="light" type="string" required>
          浅色模式下背景的十六进制颜色代码
        </ResponseField>
        <ResponseField name="dark" type="string" required>
          深色模式下背景的十六进制颜色代码
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topbarLinks" type="TopbarLink[]">
  您想在顶部栏中包含的链接的`name`和`url`数组
  <Expandable title="TopbarLink">
    <ResponseField name="name" type="string">
    按钮的名称。

    示例：`Contact us`
    </ResponseField>
    <ResponseField name="url" type="string">
    点击按钮后的url。示例：`https://mintlify.com/docs`
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="topbarCtaButton" type="Call to Action">
  <Expandable title="Topbar Call to Action">
    <ResponseField name="type" type={'"link" or "github"'} default="link">
    Link显示一个按钮。GitHub在提供的url处显示仓库信息，包括GitHub星标数量。
    </ResponseField>
    <ResponseField name="url" type="string">
    如果是`link`：按钮链接到的位置。
    
    如果是`github`：加载GitHub信息的仓库链接。
    </ResponseField>
    <ResponseField name="name" type="string">
    按钮内的文本。仅当`type`为`link`时需要。
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="versions" type="string[]">
  版本名称数组。仅当您想在导航栏中显示带有下拉菜单的不同文档版本时使用。
</ResponseField>

<ResponseField name="anchors" type="Anchor[]">
  锚点数组，包括`icon`、`color`和`url`。
  <Expandable title="Anchor">
    <ResponseField name="icon" type="string">
    用于展示锚点的图标名称。

    示例：`comments`
    </ResponseField>
    <ResponseField name="name" type="string">
    锚点标签的名称。

    示例：`Community`
    </ResponseField>
    <ResponseField name="url" type="string">
      标记哪些页面进入锚点的URL开头。通常，这是您放置页面的文件夹名称。
    </ResponseField>
    <ResponseField name="color" type="string">
      锚点图标背景的十六进制颜色。如果传递一个带有`from`和`to`属性的对象，每个属性都是十六进制颜色，也可以是渐变色。
    </ResponseField>
    <ResponseField name="version" type="string">
      如果您想在选择正确的文档版本之前隐藏锚点，则使用此选项。
    </ResponseField>
    <ResponseField name="isDefaultHidden" type="boolean" default="false">
      如果您想隐藏锚点，直到您直接链接到其中的文档，则传递`true`。
    </ResponseField>
    <ResponseField name="iconType" default="duotone" type="string">
      图标类型
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="topAnchor" type="Object">
  覆盖最顶层锚点的默认配置。
  <Expandable title="Object">
    <ResponseField name="name" default="Documentation" type="string">
      最顶层锚点的名称
    </ResponseField>
    <ResponseField name="icon" default="book-open" type="string">
      图标名称
    </ResponseField>
    <ResponseField name="iconType" default="duotone" type="string">
      图标类型
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tabs" type="Tabs[]">
  导航选项卡的数组。
  <Expandable title="Tabs">
    <ResponseField name="name" type="string">
      选项卡标签的名称。
    </ResponseField>
    <ResponseField name="url" type="string">
      标记哪些页面进入选项卡的URL开头。通常，这是您放置页面的文件夹名称。
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="api" type="API">
  API设置的配置。在[API组件](/api-playground/demo)了解更多关于API页面的信息。
  <Expandable title="API">
    <ResponseField name="baseUrl" type="string">
      所有API端点的基本url。如果`baseUrl`是一个数组，它将启用用户可以切换的多个基本url选项。
    </ResponseField>

    <ResponseField name="auth" type="Auth">
      <Expandable title="Auth">
        <ResponseField name="method" type='"bearer" | "basic" | "key"'>
          用于所有API端点的认证策略。
        </ResponseField>
        <ResponseField name="name" type="string">
        在API playground中使用的认证参数的名称。

        如果方法是`basic`，格式应为`[usernameName]:[passwordName]`
        </ResponseField>
        <ResponseField name="inputPrefix" type="string">
        设计为认证输入字段前缀的默认值。

        例如，如果`inputPrefix`为`AuthKey`，则认证字段的默认输入结果将继承为`AuthKey`。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="playground" type="Playground">
      API playground的配置

      <Expandable title="Playground">
        <ResponseField name="mode" default="show" type='"show" | "simple" | "hide"'>
          playground是显示、隐藏，还是仅显示端点而没有添加用户交互性`simple`

          在[playground指南](/api-playground/demo)了解更多
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="maintainOrder" type="boolean">
      启用此标志可确保OpenAPI页面中的键顺序与OpenAPI文件中定义的键顺序匹配。

      <Warning>此行为很快将默认启用，届时此字段将被弃用。</Warning>
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="openapi" type="string | string[]">
  指向您的OpenAPI文件的URL或相对路径的字符串或字符串数组。
  
  示例：
  <CodeGroup>
    ```json Absolute
    "openapi": "https://example.com/openapi.json"
    ```
    ```json Relative
    "openapi": "/openapi.json"
    ```
    ```json Multiple
    "openapi": ["https://example.com/openapi1.json", "/openapi2.json", "/openapi3.json"]
    ```
  </CodeGroup>

</ResponseField>

<ResponseField name="footerSocials" type="FooterSocials">
  社交媒体账户的对象，其中键:属性对表示社交媒体平台和账户url。
  
  示例： 
  ```json
  {
    "x": "https://x.com/mintlify",
    "website": "https://mintlify.com"
  }
  ```
  <Expandable title="FooterSocials">
    <ResponseField name="[key]" type="string">
    以下值之一：`website`, `facebook`, `x`, `discord`, `slack`, `github`, `linkedin`, `instagram`, `hacker-news`
    
    示例：`x`
    </ResponseField>
    <ResponseField name="property" type="string">
    社交平台的URL。
    
    示例：`https://x.com/mintlify`
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="feedback" type="Feedback">
  启用反馈按钮的配置

  <Expandable title="Feedback">
    <ResponseField name="suggestEdit" type="boolean" default="false">
    启用一个按钮，允许用户通过拉取请求建议编辑
    </ResponseField>
    <ResponseField name="raiseIssue" type="boolean" default="false">
    启用一个按钮，允许用户提出关于文档的问题
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="modeToggle" type="ModeToggle">
  自定义深色模式切换。
  <Expandable title="ModeToggle">
    <ResponseField name="default" type={'"light" or "dark"'}>
      设置是否总是为新用户显示浅色或深色模式。未设置时，我们默认与用户操作系统的模式相同。
    </ResponseField>
    <ResponseField name="isHidden" type="boolean" default="false">
      设置为true以隐藏深色/浅色模式切换。您可以将`isHidden`与`default`结合使用，强制您的文档仅使用浅色或深色模式。例如：
      
      <CodeGroup>
      ```json Only Dark Mode
      "modeToggle": {
        "default": "dark",
        "isHidden": true
      }
      ```

      ```json Only Light Mode
      "modeToggle": {
        "default": "light",
        "isHidden": true
      }
      ```
      </CodeGroup>

    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="backgroundImage" type="string">
  显示在每个页面背后的背景图片。参见[Infisical](https://infisical.com/docs)和[FRPC](https://frpc.io)的示例。
</ResponseField>